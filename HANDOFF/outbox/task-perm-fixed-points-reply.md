Done.

- Added `permClass.numFixedPoints : Parameter permClass`.
- Added the `jointCount` sanity examples for `(n, k) = (0, 0)` and `(1, 1)` with complete proofs.
- No placeholder proofs in `AnalyticCombinatorics/PartA/Ch2/LabelledClass.lean`.
- Verified with `lake build`.
