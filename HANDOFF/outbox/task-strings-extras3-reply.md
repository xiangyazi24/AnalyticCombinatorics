Done.

Modified `AnalyticCombinatorics/Examples/Strings.lean` by appending binary, ternary, and quaternary string count sanity checks for n = 23, 24, 25 using the requested direct proof shape.

Verification:
- `lake env lean AnalyticCombinatorics/Examples/Strings.lean`
- `lake build AnalyticCombinatorics.Examples.Strings`

Note: a full `lake build` was started and reached `AnalyticCombinatorics.Examples.Strings` successfully, but the session did not return further output and the sandbox did not permit interrupting or inspecting that process. The file/module build target returned green.
