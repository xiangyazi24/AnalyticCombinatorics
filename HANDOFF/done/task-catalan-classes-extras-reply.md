Implemented the requested Catalan sanity extensions:

- `PlaneTrees.lean`: added `planeTreeClass.count 9..12`.
- `Triangulations.lean`: added `triangulationClass.count 9..12`.
- `DyckPaths.lean`: added `dyckPathClass.count 6..10`.

Verification: `lake build` completed successfully.
