Done.

- Added `padovanNumParts : Parameter padovanClass`.
- Added the requested small bivariate `jointCount` sanity examples.
- Verified with `lake env lean AnalyticCombinatorics/Examples/Padovan.lean`.
- Verified full build with `lake build`.
