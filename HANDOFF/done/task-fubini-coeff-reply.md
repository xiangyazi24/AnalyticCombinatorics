Done.

- Appended the requested coefficient-form Fubini example to `AnalyticCombinatorics/Examples/SetPartitions.lean`.
- Reused existing theorem `labelSeq_posIntClass_count_eq_fubini`.
- Verified with `lake env lean AnalyticCombinatorics/Examples/SetPartitions.lean`.
- Verified full build with `lake build`.
- No new `sorry`/`admit` in `SetPartitions.lean`.
