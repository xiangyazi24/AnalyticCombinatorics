Done.

- Added the extra `compositionClass.count` sanity checks.
- Added `compositionClass_count_eq_pow_pred` for `n >= 1`.
- Verified with `lake env lean AnalyticCombinatorics/Examples/Compositions.lean`.
- Verified with `lake build`.
