Created `AnalyticCombinatorics/PartA/Ch2/Species.lean`.

Contents:
- counting-level species representation by sequences `ℕ → ℕ`
- atom, set, cycle, linear-order, and partition species counts
- species sum and labelled product counts
- EGF coefficient check for labelled linear orders / `SEQ`
- `PERM = SET(CYC)` counting check via `cycleTypeCount`, including explicit checks for `n = 0..6`

Verification:
- `lake build AnalyticCombinatorics.PartA.Ch2.Species` passed.
