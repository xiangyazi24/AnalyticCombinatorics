# Task — Derangements sanity beyond n=27

**File:** `AnalyticCombinatorics/Examples/Derangements.lean` (append)

Extend `derangementClass.count n` sanity through n=30. Compute correct values yourself — verify with `lake env lean` before writing the reply.

Use the same proof shape (`rw [derangementClass_count_eq_numDerangements]; decide`).

## Hard constraints

- Build green. No new sorrys.
- Reply at HANDOFF/outbox/task-derange-extras7-reply.md.
- **ONLY modify `AnalyticCombinatorics/Examples/Derangements.lean`.**
