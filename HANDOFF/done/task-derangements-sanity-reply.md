Done.

- Appended sanity checks for `derangementClass.count 0..6 = 1, 0, 1, 2, 9, 44, 265` in `AnalyticCombinatorics/Examples/Derangements.lean`.
- Used the existing bridge `derangementClass_count_eq_numDerangements`.
- `lake env lean AnalyticCombinatorics/Examples/Derangements.lean` passes.
- `lake build` passes.

No new `sorry`s.
