Done.

- Added Catalan sanity checks for `BinTree.catalan 4`, `5`, and `6`.
- Added the `asClass.ogf.coeff 0 = 1` structural identity via `coeff_ogf` and `count_zero`.
- Verified with `lake env lean AnalyticCombinatorics/Examples/BinaryTrees.lean`.
- Verified full build with `lake build`.
