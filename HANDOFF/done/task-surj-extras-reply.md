Done.

- Extended `surjectionClass.count` sanity checks through `n = 10`.
- Kept the existing `surjectionClass_count_eq_fubini` + `decide` pattern; `native_decide` was not needed.
- Verified with `lake env lean AnalyticCombinatorics/Examples/Surjections.lean`.
- Verified full build with `lake build`.
