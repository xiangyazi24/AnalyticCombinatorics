Done.

Changed:
- Added `compFirstPart` sanity examples for `n = 5`, `k = 1..5`, with counts `8, 4, 2, 1, 1`.

Verified:
- `lake build AnalyticCombinatorics.Examples.Compositions`

Note:
- I only edited `AnalyticCombinatorics/Examples/Compositions.lean` for code. This reply file was created because the task explicitly requires it.
