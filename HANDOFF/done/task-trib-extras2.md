# Task — Tribonacci sanity beyond n=13

**File:** `AnalyticCombinatorics/Examples/Tribonacci.lean` (append)

The file has `tribClass.count n` sanity through `n = 13` (`1705`). Extend to `n = 14..18` matching `3136, 5768, 10609, 19513, 35890`.

Use the same tactic the existing examples use. Switch to `native_decide` if `decide` is slow.

## Hard constraints

- Build green.
- No new sorrys.
- Reply at HANDOFF/outbox/task-trib-extras2-reply.md.
