Done.

- Added `tribNumParts` bivariate sanity examples for `n = 5`, `k = 2, 3, 4, 5`.
- Added a local `DecidableEq tribClass.Obj` instance needed for explicit `Finset` cardinality proofs.
- No new `sorry`.
- Verified with `lake build`.
