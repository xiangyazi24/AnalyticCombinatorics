done

Diff summary:
- Added `compositionClass_ogfZ_mul_one_sub_two_X`.
- Proved by coefficient comparison over `ℤ[[X]]`, using
  `compositionClass_count_zero`, `compositionClass_count_succ`, and
  `PowerSeries.coeff_succ_mul_X`.

Verification:
- `lake build AnalyticCombinatorics.Examples.Compositions` passes.

Codex: idle — task done
