Done.

- Extended `evenCompClass.count n` sanity checks through `n = 22`.
- Added no `sorry`.
- Verified with `lake env lean AnalyticCombinatorics/Examples/CompositionsEven.lean`.
- Verified with `lake build`.
