Done.

- Added `tribClass.count n` sanity examples for `n = 8..13` with values `81, 149, 274, 504, 927, 1705`.
- Used the existing recurrence/`rw`/`decide` style.
- Verified with `lake env lean AnalyticCombinatorics/Examples/Tribonacci.lean`.
- Verified full build with `lake build`.
