Done.

- Added `tetraNumParts : Parameter tetraClass`.
- Added small `jointCount` sanity examples for `(n, k)` through `n = 4`.
- Verified with `lake env lean AnalyticCombinatorics/Examples/Tetranacci.lean`.
- Verified full build with `lake build`.
- No new `sorry`/`admit` in `AnalyticCombinatorics/Examples/Tetranacci.lean`.
