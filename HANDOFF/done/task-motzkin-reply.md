Done.

- Added `AnalyticCombinatorics/Examples/MotzkinTrees.lean`.
- Defined `MotzTree`, internal-node `size`, `asClass`, and proved finite levels with no `sorry`.
- Added count setup: `count`, `count_zero`, `count_succ`, OGF equation `M = 1 + X * M + X * M^2`, and small sanity examples.
- Added root import in `AnalyticCombinatorics.lean`.
- Verified with `lake build AnalyticCombinatorics.Examples.MotzkinTrees AnalyticCombinatorics`.

No blocker.
