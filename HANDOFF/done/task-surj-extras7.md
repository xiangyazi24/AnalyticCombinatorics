# Task — Surjections sanity beyond n=23

**File:** `AnalyticCombinatorics/Examples/Surjections.lean` (append)

Extend `surjectionClass.count n` (Fubini/ordered Bell numbers) through n=26. Compute correct values yourself — verify with `lake env lean` before writing the reply.

Use the same proof shape (`rw [surjectionClass_count_eq_fubini]; native_decide`).

## Hard constraints

- Build green. No new sorrys.
- Reply at HANDOFF/outbox/task-surj-extras7-reply.md.
- **ONLY modify `AnalyticCombinatorics/Examples/Surjections.lean`.**
