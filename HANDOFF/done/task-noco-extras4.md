# Task — NoConsecutiveOnes sanity beyond n=18

**File:** `AnalyticCombinatorics/Examples/StringsNoConsecutiveOnes.lean` (append)

The file has `noConsecOnesClass.count n` sanity through n=18. Extend through n=21 (or as high as feasible).

These counts equal Fibonacci numbers: count(n) = F(n+2). F(20)=6765, F(21)=10946, F(22)=17711, F(23)=28657.

Use the same proof patterns already in the file.

## Hard constraints

- Build green.
- No new sorrys.
- Reply at HANDOFF/outbox/task-noco-extras4-reply.md.
- **ONLY modify `AnalyticCombinatorics/Examples/StringsNoConsecutiveOnes.lean`.** Local instances if needed.
