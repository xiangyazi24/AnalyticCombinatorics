Done.

- Appended cyclic Fubini sanity examples for `CombinatorialClass.labelCycCount posIntClass` at sizes 5, 6, and 7.
- Used the existing bridge theorem `labelCycCount_posIntClass_eq_cyclic_fubini`.
- Proved the concrete values with `norm_num` rather than `native_decide`.
- Verified with `lake env lean AnalyticCombinatorics/Examples/SetPartitions.lean`.
- Verified with `lake build`.
