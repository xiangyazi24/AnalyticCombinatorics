已完成。

- 追加了两个 `stringClass.disjSum stringClass` 的小 sanity examples。
- `lake env lean AnalyticCombinatorics/Examples/Strings.lean` 通过。
- `lake build` 通过；输出只有既有 linter warnings。
- `AnalyticCombinatorics/Examples/Strings.lean` 中没有 `sorry`/`admit`。
