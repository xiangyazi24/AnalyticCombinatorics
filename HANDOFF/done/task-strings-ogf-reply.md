Done.

- Added `stringClass_ogfZ_mul_one_sub_two_X` at the end of `AnalyticCombinatorics/Examples/Strings.lean`.
- Proof uses coefficient extensionality, the same `X`-shift pattern as compositions, and `stringClass_count_eq_pow`.
- Verified with:
  - `lake env lean AnalyticCombinatorics/Examples/Strings.lean`
  - `lake build`

`lake build` completed successfully. Existing style-linter warnings remain unrelated.
