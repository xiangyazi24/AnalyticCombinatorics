Done.

- Appended plane tree sanity checks for counts 7 and 8 in `AnalyticCombinatorics/Examples/PlaneTrees.lean`.
- Used the existing `norm_num [_root_.catalan_eq_centralBinom_div, Nat.centralBinom, Nat.choose]` pattern because plain `decide` does not reduce `catalan 7/8`.
- `lake build` completed successfully.
- No new `sorry`/`admit` in `PlaneTrees.lean`.
