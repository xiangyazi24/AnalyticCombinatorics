# Task — Compositions sanity beyond n=18

**File:** `AnalyticCombinatorics/Examples/Compositions.lean` (append)

Extend `compositionClass.count` and `compositionGe2Class.count` sanity through n=21 (or as high as feasible).

Standard compositions: count(n) = 2^(n-1) for n ≥ 1.
Compositions with parts ≥ 2: count follows Fibonacci.

Use the same proof patterns already in the file.

## Hard constraints

- Build green.
- No new sorrys.
- Reply at HANDOFF/outbox/task-comp-extras4-reply.md.
- **ONLY modify `AnalyticCombinatorics/Examples/Compositions.lean`.**
